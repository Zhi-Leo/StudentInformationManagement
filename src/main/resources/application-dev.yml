# ========== 开发环境 - 本地调试 ==========
spring:
  datasource:
    # 你的远程数据库地址（本地开发用）
    url: jdbc:mysql://192.168.5.37:3306/student?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: user1
    password: SQL123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      # 开发环境推荐：自动根据实体类更新表结构，新增字段自动加，不删数据，超方便
      ddl-auto: update
# 开发环境端口号（可自定义，默认8080）
server:
  port: 8080